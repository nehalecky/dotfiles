# Managed by chezmoi - do not edit directly
# Profile: {{ .profile }}
# Generated: {{ now | date "2006-01-02" }}

tap "romkatv/powerlevel10k"

# ── Core Development ────────────────────────────────────────
brew "bash"
brew "bat"
brew "chezmoi"
brew "delta"
brew "eza"
brew "fd"
brew "fzf"
brew "gh"
brew "git"
brew "glow"
brew "jq"
brew "just"
brew "lazygit"
brew "node" # candidate for mise management when Descript toolchain is adopted
brew "ripgrep"
brew "starship"
brew "tealdeer"
brew "uv"
brew "wget"

# ── Shell & Terminal ────────────────────────────────────────
brew "yazi"
brew "zellij"
brew "zsh"

# ── System Utilities ────────────────────────────────────────
brew "btop"
brew "dust"
brew "procs"

# ── Containers & Infra ──────────────────────────────────────
brew "lazydocker"

# ── macOS Applications - All Profiles ──────────────────────
cask "1password"
cask "1password-cli" # CLI tool, distributed as cask by AgileBits
cask "claude"
cask "cursor"
cask "raycast"

# ── Fonts & Resources ───────────────────────────────────────
cask "font-awesome-terminal-fonts"
cask "font-meslo-lg-nerd-font"

{{ if eq .terminal "wezterm" -}}
# ── Terminal: WezTerm ───────────────────────────────────────
cask "wezterm"
{{ else if eq .terminal "iterm2" -}}
# ── Terminal: iTerm2 ────────────────────────────────────────
cask "iterm2"
{{ else if eq .terminal "ghostty" -}}
# ── Terminal: Ghostty ───────────────────────────────────────
cask "ghostty"
{{ else if eq .terminal "kitty" -}}
# ── Terminal: Kitty ─────────────────────────────────────────
cask "kitty"
{{ else if eq .terminal "alacritty" -}}
# ── Terminal: Alacritty ─────────────────────────────────────
cask "alacritty"
{{ end -}}

{{ if .isWork -}}
# ── Work: Platform Engineering ──────────────────────────────
brew "cosign"
brew "helm"
brew "k9s"
brew "kubectl"
brew "kustomize"
cask "docker"
cask "google-cloud-sdk"

# ── Work: Applications ─────────────────────────────────────
cask "slack"
cask "zoom"
{{ end -}}

{{ if .isPersonal -}}
# ── Personal: Development Tools ─────────────────────────────
brew "emacs", restart_service: :changed
brew "fabric-ai"
brew "helix"
brew "llm"
brew "marksman"

# ── Personal: Productivity & Task Management ────────────────
brew "task"
brew "taskwarrior-tui"

# ── Personal: API & Networking ──────────────────────────────
brew "atac"
brew "bandwhich"

# ── Personal: Applications ──────────────────────────────────
cask "arc"
cask "firefox"
cask "github"
cask "obsidian"
cask "pokemon-tcg-live"
cask "protonvpn"
cask "signal"
cask "sonic-pi"
cask "spotify"
cask "steam"
cask "telegram"
cask "visual-studio-code"
cask "vlc"
cask "whatsapp"
cask "zotero"

# ── Personal: VS Code Extensions ────────────────────────────
vscode "davidanson.vscode-markdownlint"
vscode "github.copilot"
vscode "github.copilot-chat"
vscode "ms-python.debugpy"
vscode "ms-python.python"
vscode "ms-python.vscode-pylance"
vscode "ms-toolsai.jupyter"
vscode "ms-toolsai.jupyter-keymap"
vscode "ms-toolsai.jupyter-renderers"
vscode "ms-toolsai.vscode-jupyter-cell-tags"
vscode "ms-toolsai.vscode-jupyter-slideshow"
vscode "shd101wyy.markdown-preview-enhanced"
vscode "yzhang.markdown-all-in-one"
{{ end -}}
