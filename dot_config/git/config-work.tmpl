{{ if eq .profile "work" -}}
# Work-specific git identity — loaded via includeIf when cwd is under ~/work/descript/.
# Managed by chezmoi.
# TODO(work-device): verify gitdir path matches actual Descript repo layout (#follow-up).
# TODO(work-device): set signingkey if Descript requires a separate key (#follow-up).
[user]
    email = {{ dig "work_git_email" "TODO-WORK-EMAIL" . }}
{{ end -}}
