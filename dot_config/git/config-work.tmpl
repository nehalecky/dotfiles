{{ if eq .profile "work" -}}
# Work-specific git identity — loaded via includeIf for repos under ~/Projects/.
# Managed by chezmoi.
[user]
    email = {{ dig "work_git_email" .git_email . }}
    signingkey = {{ .git_signing_key }}
{{ end -}}
